## Packages
framer-motion | Page transitions and scroll animations
lucide-react | Iconography
react-hook-form | Form handling
@hookform/resolvers | Validation resolvers for hook form

## Notes
- Tailwind config needs to support custom fonts: Outfit (headings) and DM Sans (body)
- Images will use Unsplash for placeholders where specific assets aren't available
- API endpoints are defined in shared/routes.ts and must be strictly followed
